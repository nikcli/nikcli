sst-env.d.ts
# GitHub workflows must not be modified during release `prettier --write .`
# (causes "refusing to allow a GitHub App to create or update workflow ... without `workflows` permission"
# on push, since GITHUB_TOKEN cannot push to .github/workflows/*.yml by design).
.github/workflows/
.github/actions/
install
packages/web/install
# Expo CNG output. Gitignored, but `prettier --write .` still walks it and dies
# on the multi-GB Xcode compilation cache, so ./script/format.ts fails for
# anyone who has built the iOS app locally.
packages/mobile/ios/
packages/mobile/android/

# Generated by `wrangler types`, which verifies its own output with a checksum.
# Reformatting it makes `wrangler types --check` report the types as out of date,
# which fails `turbo typecheck` for the whole monorepo.
**/worker-configuration.d.ts

# Written by packages/httpapi-codegen (JSON.stringify(..., null, 2)) and compared
# byte-for-byte by the CI "Generated HTTP client drift" gate. Prettier collapses the
# array onto one line, which makes the next codegen run show a diff and fail CI.
**/.httpapi-codegen.json

# Written by packages/ade/scripts/gen-frame-script.ts and pinned byte-for-byte by
# `frame-script.test.ts` (`expect(file).toBe(frameScript())`). The release runs
# `script/format.ts` (`prettier --write .`) and folds the result into the
# `release: v*` commit, so reformatting this file silently reverts every
# regeneration and fails that test on the tag — which is the only ref
# `ade / checks / test` ever looks at. Verified 2026-09-18: prettier's output is
# byte-identical at v1.369.0 and v1.370.0.
packages/ade/src-tauri/scripts/browser-frame.js
